package androidx.compose.ui.graphics.layer;

import kotlin.Metadata;
import kotlin.coroutines.Continuation;

@Metadata
public interface LayerSnapshotImpl {
    Object a(GraphicsLayer graphicsLayer, Continuation continuation);
}
