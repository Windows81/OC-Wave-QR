package androidx.core.content;

import androidx.core.util.Consumer;
import kotlin.Metadata;

@Metadata
public interface OnConfigurationChangedProvider {
    void g(Consumer consumer);

    void u(Consumer consumer);
}
