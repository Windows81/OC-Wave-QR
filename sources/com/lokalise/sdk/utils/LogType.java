package com.lokalise.sdk.utils;

import kotlin.Metadata;

@Metadata
public enum LogType {
    API,
    PRE_INFLATION,
    POST_INFLATION,
    SDK_MENU_INFLATER,
    SDK_XML_INFLATER,
    TRANSLATION,
    SDK,
    REALM
}
